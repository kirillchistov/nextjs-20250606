export default function SignInPage() {
  return (
    <p>
      Authentication is disabled in the GitHub Pages demo. Run the project locally
      with the webinar server to try sign-in.
    </p>
  );
}
